<% provide(:title, "Courses Home Page") %>
<h1>Courses Home Page</h1>
<ul>
    <% @courses.each do |course| %>
        <li>
            <%= course.crn %>
            <%= course.title %>
            <%= course.section %>
        </li>
    
    <% end %>
</ul>